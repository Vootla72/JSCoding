console.log({ a: 1 } == { a: 1 }); //o/p:false
console.log({ a: 1 } === { a: 1 }); //o/p:false
//both are them are different objects
//both of them have different space in the memory
//objects are only equal when they are referencing to particular space in the memory
//here both are independent objects
